#!/usr/bin/env python
# -*- coding: UTF-8 -*-
def centerfig(f,w,h):
    '''#STARTCODE
        scsize = get( groot, 'Screensize' );
        sw = scsize(3);
        sh = scsize(4);
        x = (sw-w)/2;
        y = (sh-h)/2;
        set(f,'Position',[x y w h]);

    '''#STOPCODE
